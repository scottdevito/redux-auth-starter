import { fetchSomething, selectSomething } from './action-fetch';
import {
  login,
  register,
  logout,
  createNewUser,
  loginPersist,
} from './action-login';

export {
  fetchSomething,
  selectSomething,
  login,
  register,
  logout,
  createNewUser,
  loginPersist,
};
